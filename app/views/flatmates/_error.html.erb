<% if @flatmate.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@flatmate.errors.count, "error") %> </h2>

    <ul>
      <% @flatmate.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
